#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - absolute value
 * @n: integer type
 *
 * description: a function that computes the absolute value of an integer.
 *
 * Return: Always 0
*/

int _abs(int n)
{
	int x = abs(n);
	return (x);
}
